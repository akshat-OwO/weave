# Weave docs

The Weave documentation site is a Next.js application built with [Fumadocs](https://fumadocs.dev).

Run it from the repository root:

```bash
bun install
bun run --cwd apps/docs dev
```

Then open <http://localhost:3000>.
