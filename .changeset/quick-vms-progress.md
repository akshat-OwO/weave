---
"@weave/cli": patch
---

Speed up repeat VM creation with three-day cached bases and a `--fresh` bypass, add distro-neutral package and container-tooling progress, and keep Lima logs contained when `shell` or `ssh` starts a stopped VM.
