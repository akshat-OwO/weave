---
"@weave/cli": patch
---

Add `weave cp` for copying host files into a VM without writable host mounts, with direct transfers to writable destinations and elevated staging for protected guest paths.
